#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @a: first interger
 * @b: second interger
 * Return: the multiplication of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
